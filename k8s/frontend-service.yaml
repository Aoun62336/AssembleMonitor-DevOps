apiVersion: v1
kind: Service
metadata:
  name: assemblemonitor-frontend-service
  namespace: assemblemonitor
spec:
  type: NodePort
  selector:
    app: assemblemonitor-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080