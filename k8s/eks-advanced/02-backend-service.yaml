apiVersion: v1
kind: Service
metadata:
  name: assemblemonitor-backend-service
  namespace: assemblemonitor
spec:
  type: ClusterIP
  selector:
    app: assemblemonitor-backend
  ports:
    - name: http
      port: 8000
      targetPort: 8000
